const Profile = () => (
  <div
    style={{
      width: "100%",
      minHeight: "80vh",
      background: "var(--white)",
      border: "2px solid var(--gray)",
      borderRadius: "5px",
    }}
  >
    Profile
  </div>
);

export default Profile;
